all:

  children:

    autoflows:

      hosts:

        autoflow1:

          ansible_host: 192.168.9.180  # penser à la notion du DNS 
          #ansible_user: worker


    #local:

      #hosts:

        #localhost:

          #ansible_connection: local
          